spring:
  datasource:
    url: jdbc:tc:mysql:8.0.28://hostname/testdbdb?user=root?password=root;
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    properties:
      hibernate:
        show_sql: true